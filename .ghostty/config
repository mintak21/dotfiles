# All Settings refer for https://ghostty.org/docs/config/reference
# Basic Settings
command = /bin/zsh
working-directory = inherit
abnormal-command-exit-runtime = 300
scrollback-limit = 50000000

# Clipboard Settings
clipboard-read = allow
clipboard-write = allow

# Background Settings
theme = Midnight In Mojave
# theme = Xcode Dark hc
background-image = ~/.config/ghostty/yumemita.png
background-image-opacity = 0.15
background-image-fit = stretch

# Font Settings
font-family = 'HackGen Console NF'
font-feature = "-calt"
font-feature = "-dlig"
font-feature = "-liga"
font-size = 15

# Cursor Settings
cursor-opacity = 0.7
cursor-style = block
cursor-style-blink = false

unfocused-split-opacity = 0.825

# Window Settings
title = Ghostty

# Update Check
auto-update = check
auto-update-channel = stable